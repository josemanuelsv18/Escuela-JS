var nombre = 'Josemanuel';
var apellido = 'Sifontes';

var nombre_mayusculas = nombre.toUpperCase();
var apellido_minusculas = apellido.toLowerCase();

var primera_letra_nombre = nombre.charAt(0);
var cantidad_letras_nombre = nombre.length;

var nombre_completo = `${nombre} ${apellido.toUpperCase()}`;

var str = nombre.substr(1, 2);

var reto = nombre.charAt((cantidad_letras_nombre - 1));